{
  "name": "tag-checker",
  "version": "0.0.0",
  "scripts": {
    "test": "jest --watchAll --verbose --coverage"
  },
  "devDependencies": {
    "@types/jest": "^27.0.1"
  },
  "dependencies": {
    "jest": "^27.2.0"
  }
}
